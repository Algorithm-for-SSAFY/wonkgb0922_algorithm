# [{난이도(Silver II)}] {문제 이름} - {문제 번호} 

[문제 링크](여기에 문제 링크) 

### 성능 요약

메모리: 152880 KB, 시간: 516 ms

### 분류

백준 분류를 ','를 구분자로 작성.

### 제출 일자

2026년 m월 d일 hh:mm:ss

### 문제 설명

<p>{문제 복붙}</p>

### 입력 

 <p>{입력 복붙}</p>

### 출력 

 <p>{출력 복붙}</p>

### 💡접근 방법
> dfs로 풀었다. 정점과 연결된 다른 노드를 저장하기 위해 `List`를 배열로 선언하였고, 이 `List`가 `null`일 경우 생성자로 초기화하는 코드도 만들어주었다.
> 무방향 그래프이므로 자신과 연결된 다른 정점에도 자신을 추가해주어야 한다.
> `visited` 배열을 통해 방문 여부를 확인해주었고, 간선이 없는 경우 탐색하지 않는 대신 카운팅을 해주었다. 자기 자신도 카운트 되므로.

### 🤔 회고
> 자바로 그래프를 구현하는 건 처음이었는데, 의외로 할만할지도? 코드가 깨끗하지 않은 거는 자바 특성상 어쩔 수 없어 보인다..
